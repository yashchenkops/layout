mixin header()
  header.header
    .container
      a.header__logo(href="index.html")
        +picture({
          lazy: false,
          path: "img/logo.png",
          attr: { "alt": "Logo" },
        })